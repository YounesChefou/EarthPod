#include "mbed.h"
#include "testMMA84.h"
/*#include "MMA8451.h"

I2C i2c(D12, A6);
MMA8451 mma(i2c, 0x1C);

int main() {
    printf("WHO AM I: 0x%02X\r\n", mma.id());
    mma.setActiveMode(); //Mode actif
    while(1) {
        printf("x: %.3f  y: %.3f  z: %.3f \r", mma.getX(), mma.getY(), mma.getZ());
        wait(0.5);
    }
}*/
